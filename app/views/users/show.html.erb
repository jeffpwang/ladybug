
<% provide(:about, "active") %>
<% provide(:title, "User Log") %>

<div class="container">
   <div class="row">
    <div class="col-lg-12 text-center">
       <h2>Profile</h2>
       <hr class="star-primary">
     </div>
   </div>
   <div class="row">
     <div class="col-lg-12">
          <%= render "flash_message" %>               
          <%= render "statistics" %>          
          <%= render "logs" %>
          <%= will_paginate @logs %>
     </div>
   </div>
</div>

    <%= link_to "Delete Account", user_path(@user.id), method: "delete", class: "btn btn-danger btn-md"%>   
</div>
